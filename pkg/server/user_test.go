package server

import (
	"testing"
)

func TestServer_User(t *testing.T) {
	t.Run("ok", func(t *testing.T) {
		// req := &grpc.UserRequest{
		// 	Id: "1",
		// }
		// got, err := testServer.UnimplementedUserServiceServer.User(context.Background(), req)

		// assert.NoError(t, err)
		// assert.NotEmpty(t, got)

		// assert.Equal(t, "", got.Name)

	})

}
